const PRODUCT_DIVIDER = Object.freeze(['-']);

const VALID_ANSWERS = Object.freeze({
  YES: 'Y',
  NO: 'N',
});

const UNIT = Object.freeze({
  WON: '원',
  COUNT: '개',
});

const NON_EXIST_QUANTITY = Object.freeze(['재고 없음']);

export { PRODUCT_DIVIDER, VALID_ANSWERS, UNIT, NON_EXIST_QUANTITY };
